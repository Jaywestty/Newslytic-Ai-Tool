services:
  - type: web
    name: newslytic-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn apis.Newslytic_app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0